# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-04-14T10:49:16.287Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: reusable-readme-docs

on:
  workflow_call:
    secrets:
      readmeApiKey:
        required: true

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Determine Node.js version
        run: .github/scripts/determine-node-version.sh

      - uses: actions/setup-node@v3
        with:
          node-version: ${{ inputs.nodeVersion }}
          cache: 'yarn'

      - name: Install
        run: yarn

      - name: Generates docs 🚀
        run: yarn generate-docs

      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs docs --key=${{ secrets.readmeApiKey }} --version=1.0
