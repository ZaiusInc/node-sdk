# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-04-14T10:49:16.287Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: reusable-readme-docs

on:
  workflow_call:
    secrets:
      readmeApiKey:
        required: true

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Determine Node.js version
        id: node
        uses: ZaiusInc/node-sdk/.github/actions/determine-node-version@master

      - uses: actions/setup-node@v3
        with:
          node-version: ${{ env.node_version }}
          cache: 'yarn'

      - name: Install
        run: yarn

      - name: Generates docs 🚀
        run: yarn generate-docs

      - name: Run `docs upload` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: docs upload ./docs --key=${{ secrets.readmeApiKey }} --branch=1.0
